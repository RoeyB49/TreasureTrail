<?xml version="1.0" encoding="utf-8"?>
<resources>

        <style name="RoundedTextField" parent="Widget.MaterialComponents.TextInputLayout.OutlinedBox">
                <item name="boxCornerRadiusTopStart">16dp</item>
                <item name="boxCornerRadiusTopEnd">16dp</item>
                <item name="boxCornerRadiusBottomStart">16dp</item>
                <item name="boxCornerRadiusBottomEnd">16dp</item>
                <item name="android:layout_width">0dp</item>
                <item name="android:layout_height">wrap_content</item>
                <item name="android:paddingHorizontal">20dp</item>

        </style>

        <!-- Style for rounded button -->
        <style name="RoundedButton">
                <item name="android:layout_width">200dp</item>
                <item name="android:layout_height">wrap_content</item>
                <item name="android:backgroundTint">@color/buttonColor</item>
                <item name="android:textColor">@color/white</item>
                <item name="android:textSize">16sp</item>
                <item name="android:layout_marginTop">20dp</item>

        </style>

        <!-- Custom single-choice chip style -->
        <style name="MySingleChoiceChip" parent="@style/Widget.MaterialComponents.Chip.Choice">
                <!-- Spacing around the chip (optional) -->
                <item name="android:layout_margin">2dp</item>

                <!-- Corner radius for a rounded look -->
                <item name="chipCornerRadius">16dp</item>

                <!-- Chip stroke settings -->
                <item name="chipStrokeWidth">1dp</item>
                <item name="chipStrokeColor">@color/chip_stroke_color</item>

                <!-- Background and text colors -->
                <item name="chipBackgroundColor">@color/chip_background</item>
                <item name="android:textColor">@color/chip_text_color</item>

                <!-- Adjust text size if needed -->
                <item name="android:textSize">14sp</item>
        </style>

</resources>